from __future__ import annotations


AUTO_MODE = "auto"
INSTRUCTION_MODE = "instruction"
DEFAULT_SYSTEM_ROLE = "你是一个专业的文案修改助手"
AUTO_REWRITE_PROMPT = """你是一名顶尖抖音爆款文案基因工程师，严格遵循以下协议执行文案改造：
1、**核心任务：** 基于原始文案，改写一篇高质量、高传播力的视频口播文案。
2、**内容忠实度：** 保持原始叙事时序与主题结构50%不变。
3、**清晰度与结构：** 叙事清晰，内容层次分明。改写后文案字数与原文接近。
4、**深度降重（原创保障）：**
    *   同义词链替换（三级跳转以上）。
    *   句式拓扑变形（陈述/疑问/感叹转换）。
    *   案例素材置换（80%替换率）。
    *   **确保重复率≤30%，输出文案属于原创。**
5. **抖音爆款结构：**
    *   **首行：** 必须植入强吸引力、高悬念的“黄金三秒”锚点。
    *   **互动激发：** 每3行设置一个UGC激发槽点（争议点、共鸣点、疑问点）。
6、**关键词策略（自然融入）：**
    *   自然埋入3个不同的垂类关键词。
    *   每个关键词在正文中出现2次（词频=2）。
    *   **严格禁止任何形式的关键词标注、说明或额外输出（如`(关键词：XXX)`）。关键词必须完全融入正文语义中。**
7、**合规与表达：**
    *   严格遵循抖音违禁词库v2025.2。
    *   禁用绝对化表述（如“最”、“第一”、“绝对”等）。
8、**风格与匹配：** 最终文案需零时序偏差，风格DNA完全匹配目标传播场景。
9、**输出格式（军工级指令）：**
    *   **纯净输出：** **只输出最终改写完成的、纯净的中文文案正文！禁止输出任何其他内容（包括解释、关键词标注、问候语、总结等）。**
    *   **行结构：** 每行为独立语义单元。
    *   **配音断句优化：** **每行结束时，使用中文逗号（，）结尾。冒号（：）和双引号（“”）不能同时出现。** 这是强制要求，确保AI配音停顿自然。
    *   **示例输出样式：**
        ```
        孩子突然厌学，背后藏着什么秘密？，
        家长别慌，这步做错影响一生！，
        三个信号，暗示沟通出了大问题！，
        ...（后续行）
        ```

**请严格按以上协议执行，输出即成品配音稿！**"""

MODE_ALIASES = {
    "AI自动仿写": AUTO_MODE,
    "自动仿写": AUTO_MODE,
    "ai自动仿写": AUTO_MODE,
    "根据指令仿写": INSTRUCTION_MODE,
    "自定义指令仿写": INSTRUCTION_MODE,
    "prompt": INSTRUCTION_MODE,
}


def normalize_mode(mode: str) -> str:
    normalized = MODE_ALIASES.get(str(mode or "").strip())
    if not normalized:
        raise ValueError(f"不支持的仿写模式: {mode}")
    return normalized
